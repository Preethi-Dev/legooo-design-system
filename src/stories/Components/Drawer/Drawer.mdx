import {
  Canvas,
  Meta,
  Primary,
  Controls,
  Stories,
  Subtitle,
  Source,
} from "@storybook/blocks";
import * as DrawerStories from "./Drawer.stories";

<Meta of={DrawerStories} />

<h1 style={{ fontSize: "3rem" }}>Drawer</h1>

A panel that slides out from the edge of the screen.

# Basic

The simplest usage.

<Canvas of={DrawerStories.Basic} />

# Properties

<Controls of={DrawerStories.Basic} />

# Custom Placement

The Drawer can appear from any edge of the screen.

<Canvas of={DrawerStories.Custom} />

# Properties

<Controls of={DrawerStories.Custom} />

# Extra Actions

Extra actions should be placed at corner of drawer in Ant Design, you can use `extra` prop for that.

<Canvas of={DrawerStories.Extra} />

# Properties

<Controls of={DrawerStories.Extra} />

# Preset size

By setting `size`, The `default` width (or height) of Drawer is `378px`, and there is a preset `large` size `736px`.

<Canvas of={DrawerStories.Preset} />
