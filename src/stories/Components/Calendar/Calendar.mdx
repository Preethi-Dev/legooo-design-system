import {
  Canvas,
  Meta,
  Primary,
  Controls,
  Stories,
  Source,
} from "@storybook/blocks";
import * as CalendarStories from "./Calendar.stories";

<Meta of={CalendarStories} />

<h1 style={{ fontSize: "3rem" }}>Calendar</h1>

A container that displays data in calendar form.

# Examples

A basic calendar component with `Year`/`Month` switch.

<Primary />

<br />
<br />
<br />

# ⚛️ Atom of Calendar

- DataCell
  <br />
  <br />

# Data Cell

The `DataCell` component renders a cell within a calendar, typically representing a day, with customizable styling based on its properties.

<Canvas of={CalendarStories.Cell} />

# Properties

<Controls of={CalendarStories.Cell} />
