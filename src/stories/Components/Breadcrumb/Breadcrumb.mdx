import {
  Canvas,
  Meta,
  Primary,
  Controls,
  Stories,
  ArgTypes,
} from "@storybook/blocks";
import * as BreadcrumbStories from "./Breadcrumb.stories";

<Meta of={BreadcrumbStories} />

<h1 style={{ fontSize: "3rem" }}>Breadcrumb</h1>

A breadcrumb displays the current location within a hierarchy. It allows going back to states higher up in the hierarchy.

# Basic Usage

The simplest use

<Primary />

# Properties

<Controls />

<p style={{ fontSize: "1.5rem" }}>$items</p>

An array of objects. An object structure will be:

- **title** `string` - item name
- **icon** `string` - set icon by specify the icon name
- **dropdown** `boolean` - enable dropdown
- **href** `string`- Target of hyperlink

```jsx
{ title: "Home", icon: "HomeOutlined", dropdown: false }
```

<br />
<br />

# Link

<Canvas of={BreadcrumbStories.BreadcrumbItem} />

# Properties

<Controls of={BreadcrumbStories.BreadcrumbItem} />

<Stories />
import {jsx} from "react/jsx-runtime";
