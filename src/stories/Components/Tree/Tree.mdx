import {
  Canvas,
  Meta,
  Primary,
  Controls,
  Stories,
  Source,
} from "@storybook/blocks";
import * as TreeStories from "./Tree.stories";

<Meta of={TreeStories} />

<h1 style={{ fontSize: "3rem" }}>Tree</h1>

Multiple-level structure list.

# Basic

The most basic usage

<Canvas of={TreeStories.Basic} />

# Properties

<Controls of={TreeStories.Basic} />

# Controlled Tree

control expand by default using `defaultExpandKeys`

<Canvas of={TreeStories.Controlled} />

# Properties

<Controls of={TreeStories.Controlled} />

# Change Switcher Icon

By specify `switcherIcon` having a three options `"square"`, `"cheveron"`, `"caret"`

<Canvas of={TreeStories.ChangeSwitcher} />

# Properties

<Controls of={TreeStories.ChangeSwitcher} />

# Toggle Icon

Enable `showIcon` which render tree with icons if icons specified in tree's data structure

<Canvas of={TreeStories.ToggleIcon} />

# Properties

<Controls of={TreeStories.ToggleIcon} />
