import {
  Canvas,
  Meta,
  Primary,
  Controls,
  Stories,
  Source,
} from "@storybook/blocks";
import * as ImageStories from "./Image.stories";

<Meta of={ImageStories} />

<h1 style={{ fontSize: "3rem" }}>Image</h1>

Preview-able image.

# Basic Usage

Click the image to zoom in.

<Canvas of={ImageStories.Basic} />

# Properties

<Controls of={ImageStories.Basic} />

# Fault tolerant

Load failed to display image placeholder.

<Canvas of={ImageStories.Fault} />

# Custom preview image

You can set different preview image.

<Canvas of={ImageStories.CustomPreview} />

# Properties

<Controls of={ImageStories.CustomPreview} />

# Disable preview image

Turn On / Off the preview image

<Canvas of={ImageStories.DisablePreview} />

# Properties

<Controls of={ImageStories.DisablePreview} />

# Blur preview image

You can blur the preview image

<Canvas of={ImageStories.BlurImage} />

# Properties

<Controls of={ImageStories.BlurImage} />
